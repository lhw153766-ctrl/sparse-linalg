# 项目申报书

## 一、项目名称

sparse-linalg —— MoonBit 通用稀疏矩阵存储与求解器库

## 二、项目简介

MoonBit 生态里已经有一个稀疏线性代数实现：`hsy-bit/moonbit-circuit-solver` 的 `src/linalg` 包，4793 行非测试代码，是那个 SPICE 电路仿真器的数值后端。本项目是独立的通用稀疏线性代数包，与它在**求解器内核**上有重叠：CSR 存储与稀疏乘向量、共轭梯度、BiCGSTAB、GMRES、ILU(0) 预条件、稀疏 Cholesky、Reverse Cuthill-McKee、Matrix Market 读写。这七项对方都有，申报时不隐瞒。

差异在四处，都是对方接口里不存在的能力，依据是逐包读过的 `pkg.generated.mbti`：

1. **列存储与转置乘**。对方只有 `CsrMatrix`，转置只存在于稠密 `Matrix` 和 `ComplexMatrix` 上。本项目的 `Csc` 让 `A^T x` 变成连续内积而不是散射，非对称求解器要用它。
2. **稀疏乘法**。对方没有任何矩阵乘矩阵；`spgemm` 和 `spmm` 是零命中。
3. **标量类型泛型**。对方全程 `Double`。本项目对 `Add`/`Mul`/`Default` 泛型，因此同一份 `spgemm` 在布尔半环上算可达性、在 min-plus 半环上算最短路，`Int64` 路径保持精确装配。
4. **图算法层**。拉普拉斯、归一化拉普拉斯、连通分量、PageRank，对方都没有。

一句话定位：那个包是**一个应用的数值后端**（它的 CSR 类型只有 5 个公开方法，够电路仿真用，不够别的用——没有取对角、没有查对称、没有第二种格式）；本项目是**生态级的通用基建**。逐条对照表在仓库根目录 `COMPARISON.md`。

## 三、方向与通用性

方向属于「基础算法与数据结构」。通用性有三处具体体现，都可运行验证：

- **格式通用**：COO 装配、CSR 按行、CSC 按列与转置乘，三者 `O(nnz)` 互转。构造时校验全部结构不变量（`row_ptr` 单调、列下标递增不重复、长度自洽），不合法在边界报错。
- **标量通用**：`Double`、`Float`、`Int64` 及自定义半环共用同一套存储与运算核。`examples/reachability` 用同一份 `spgemm` 分别跑出可达性和最短路；`Int64` 装配有测试断言 `9000000000000 + 7` 不被浮点吞掉。
- **后端通用**：213 个测试在 wasm、wasm-gc、js、native 四个后端全绿；I/O 做成字符串进出而非读写文件，就是为了不依赖文件系统。

## 四、使用场景

**场景一：偏微分方程的网格求解。** 二维泊松方程在 100×100 网格上离散后有 9604 个未知数、47628 个存储项，密度 0.05%，稠密要 0.69 GiB。ILU(0) 预条件的共轭梯度 80 步收敛到相对残差 7.5e-11；稀疏 Cholesky 与它互差 3.7e-12；两者与解析解 `sin(πx)sin(πy)` 的误差都是 8.4e-5——这个数是 h=1/99 的 O(h²) 离散误差，不是求解误差。分解一次 2.53 ms，之后每次回代只要 235 µs。

**场景二：图上的可达性与最短路，不写图算法也能算。** `examples/reachability` 用同一份 `spgemm`：布尔半环上反复平方得到传递闭包，min-plus 半环上同样操作得到全源最短路。示例图里闭包发现 a 到 d 走 b、c 只要 4，而直连边是 9。min-plus 的加法单位元取正无穷，所以缺失项表示「无路」而不是「零代价」，这是同一份乘积能算最短路的关键。

**场景三：PageRank 与时间推进求解。** `examples/pagerank` 在小图上逐行打印出度/入度/秩，秩和为 1；另在 500 页、2493 条链接的生成图上排名，2474 个存储项对稠密的 250000 个。求解器支持热启动 `initial?`：在 1600 未知数的网格上，从微扰的旧解出发比从零出发快 35%（4.01 ms → 2.59 ms），这正是时间推进循环想要的。

**场景四：从真实数据集读矩阵。** Matrix Market 是 NIST 与 SuiteSparse 两个稀疏矩阵集合的交换格式。`io` 包支持 coordinate 与 array 两种形式、real/integer/pattern 三种字段、general/symmetric/skew-symmetric 三种对称性，1-based 下标与「对称只存一个三角」都处理了，写出再读回能还原。复数矩阵直接报错拒绝，不会把虚部悄悄丢掉。

## 五、核心功能

- **存储**：`Coo`（装配，重复坐标求和并给出重复计数）、`Csr`、`Csc`，构造校验全部结构不变量。
- **运算核**：`spmv`、`spmv_add`（`y = αAx + βy`）、`spmv_transpose`（`y = Aᵀx`）、`spmm`、`spgemm`（复用稠密工作区加触碰列表，重置代价 O(touched) 而非 O(ncols)）、转置、加减、缩放、`drop_zeros`、`identity`。
- **迭代解**：共轭梯度（检查 `pᵀAp` 保持正，非正定报 `UnsupportedMatrix` 而不是继续算垃圾）、BiCGSTAB、重启 GMRES（Givens 旋转 + 右预条件，旋转给出的残差估计即原系统真实残差，不必额外乘法验证收敛）。三者都支持热启动。
- **预条件与直接解**：Jacobi、ILU(0)（零填充不完全 LU，按行递增消元、原地更新）；稀疏 Cholesky（up-looking 加消去树与 reach，只读上三角，`new_checked` 先校验对称性），附 `log_determinant`。
- **重排与图**：RCM、带宽、profile；度、邻接装配、拉普拉斯、归一化拉普拉斯、连通分量、PageRank。
- **半环**：`BoolSemiring`（或/与）、`MinPlus`（min/加），证明泛型不是装饰。
- **测试**：213 个测试，四后端全绿，`moon check --deny-warn` 与 `moon fmt --check` 干净。除手算对照外，`integration/` 有一份**独立写的稠密参考实现**（扁平行主序数组、部分主元高斯消去、另一套 Cholesky），13 个随机差分测试拿它对照每个稀疏例程。这条最要紧：稀疏下标写错是静默的，`row_ptr` 错一项照样算出看着合理的数，只有第二个实现会当场翻脸。

## 六、原创或移植

原创，不是移植。没有对照任何现有项目的代码，也没有复制任何第三方源码。与 `moonbit-circuit-solver/src/linalg` 的关系已在第二节逐项披露，两者是各自独立写成的实现。

## 七、参考来源信息

实现依据公开教科书的标准算法，只参考算法描述，未参考代码：

- Timothy A. Davis, *Direct Methods for Sparse Linear Systems*, SIAM, 2006 —— 消去树、reach、up-looking 稀疏 Cholesky、计数排序式 CSR/CSC 互转。
- Yousef Saad, *Iterative Methods for Sparse Linear Systems*, 2nd ed., SIAM, 2003 —— 预条件共轭梯度、BiCGSTAB、重启 GMRES、ILU(0)。
- Matrix Market 格式规范（NIST）—— `%%MatrixMarket` 头、字段与对称性语义。
- 半环上的图算法（布尔半环传递闭包、min-plus 最短路）是标准结论，见任意算法教材。

依赖只有 `moonbitlang/core`，没有引入任何第三方包。

## 八、GitHub 链接

https://github.com/lhw153766-ctrl/sparse-linalg

已发布到 mooncakes.io：https://mooncakes.io/docs/lhw153766-ctrl/sparse-linalg

Apache-2.0。仓库根目录 `COMPARISON.md` 有逐条能力对照表（含 2552 个模块的注册表全量检索过程），`docs/development-log.md` 记录了设计取舍、开发中发现的六个 bug 及各自的发现方式、以及已知局限。
