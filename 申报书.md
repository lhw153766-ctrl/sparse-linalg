# 项目申报书

## 一、项目名称

sparse-linalg —— MoonBit 稀疏矩阵存储与求解器库

## 二、项目简介

给做数值计算的人用。当矩阵大到存不下时，稠密表示就没法用了：10 万行 10 万列、每行约十个非零元的矩阵，稠密存下来要 80 GB 左右，压缩稀疏行（CSR）只要几 MB。这个库提供稀疏的存储格式、稀疏的运算核，以及能在这种矩阵上真正跑出结果的求解器。

mooncakes.io 上现有的 MoonBit 数值库都是稠密的。我把名字、关键词或简介里出现矩阵/线性代数/求解器/分解这类词的模块全列了出来（51 个），逐个下载后读它们的生成接口 `pkg.generated.mbti` 按存储类型分类，不按简介的说法分类：`Luna-Flow/linear-algebra` 的公开接口只有 `Matrix`（连续内存），官方精选列表对它的描述原文是 "mutable and immutable **dense** representations"；`xunyoyo/linalg` 是 `Array[Array[Double]]` 的行列表，提供稠密的 `cholesky`/`eigen`/`qr`/`svd`/`lstsq`，全模块没有 sparse 符号；`amor2025/moonNum` 的 `cholesky`/`solve`/`svd` 全部吃稠密 `NdArray`。`CAIMEOX/symbit` 里有 `SparseMatrix`，但元素类型是符号表达式 `Expr`，是计算机代数不是浮点计算；`Luna-Flow/luna-poly` 名字里也有 sparse，但那是稀疏多项式不是稀疏矩阵。数值稀疏这块在 MoonBit 生态里是空的，这个库来填。

## 三、方向与通用性

方向属于「基础算法与数据结构」。稀疏矩阵是教科书范围的东西，不是某个应用领域的专用件：偏微分方程离散化、图算法、电路仿真、最小二乘、推荐系统都用它。

通用性体现在三处。一是存储格式通用：COO 用于装配，CSR 用于按行遍历，CSC 用于按列遍历和 `A^T x`，三者之间可以 `O(nnz)` 互转。二是标量类型通用：存储和运算核只要求 `Add`/`Mul`/`Default` 这些核心 trait，所以 `Double`、`Float`、`Int64` 都能用，`Int64` 走装配还保持精确（有测试断言 `9000000000000 + 7` 不被浮点吞掉）。三是后端通用：测试在 wasm、wasm-gc、js、native 四个后端全绿，I/O 做成字符串进出而不是读写文件，就是为了不依赖文件系统。

## 四、使用场景

**场景一：偏微分方程的网格求解。** 二维泊松方程在 100×100 网格上离散后有 9604 个未知数、47628 个存储项，密度 0.05%，稠密要 0.69 GiB。用 `examples/poisson`：ILU(0) 预条件的共轭梯度 80 步收敛到相对残差 7.5e-11；稀疏 Cholesky 直接分解后因子有 941289 项，两种解法互差 3.7e-12，与解析解 `sin(πx)sin(πy)` 的误差都是 8.4e-5——这个数是 `h=1/99` 的 O(h²) 离散误差，不是求解误差。

**场景二：图上的迭代计算。** PageRank 的幂迭代每轮就是一次稀疏乘向量。`examples/pagerank` 在 8 个页面 16 条链接的图上跑，结果按出度/入度逐行打印，秩和为 1；孤立节点只拿到阻尼下限 0.020979。图拉普拉斯、连通分量、邻接矩阵装配都在 `graph` 包里。

**场景三：从真实数据集读矩阵。** Matrix Market 是 NIST 和 SuiteSparse 两个稀疏矩阵集合的交换格式，所以真实矩阵就是这么来的。`io` 包支持 coordinate 和 array 两种形式、real/integer/pattern 三种字段、general/symmetric/skew-symmetric 三种对称性；1-based 下标和「对称只存一个三角」这两个坑都处理了，写出去再读回来能还原原矩阵。复数矩阵直接报错拒绝，不会把虚部悄悄丢掉。

## 五、核心功能

- **存储**：COO/CSR/CSC 三种格式，构造时校验全部结构不变量（`row_ptr` 单调、列下标递增且不重复、长度自洽），不合法就在边界处报错，而不是留到后面算出看着合理的错数。
- **运算核**：`spmv`（`y = Ax`）、`spmv_add`（`y = αAx + βy`）、`spmv_transpose`（`y = Aᵀx`）、稀疏×稠密 `spmm`、稀疏×稀疏 `spgemm`（用复用的稠密工作区加触碰列表，重置代价 O(touched) 而非 O(ncols)）、转置、加减、按比例缩放。
- **迭代解**：共轭梯度（检查 `pᵀAp` 保持正，不是正定就报 `UnsupportedMatrix` 而不是继续算出垃圾）、BiCGSTAB、重启 GMRES（Givens 旋转 + 右预条件，所以旋转给出的残差估计就是原系统的真实残差，不需要额外一次乘法去验证收敛）。
- **预条件**：Jacobi、ILU(0)（零填充不完全 LU，按行递增消元、原地更新，因为消第 i 行时读到的第 k<i 行已经定型）。
- **直接解**：稀疏 Cholesky，up-looking 算法 + 消去树 + reach 计算，只读 A 的上三角。附带 `log_determinant`。
- **重排**：Reverse Cuthill-McKee，以及带宽、profile 度量。60×60 网格拉普拉斯上，把打乱编号的矩阵从带宽 3558 恢复到 60，因子项数 1072317 降到 149330。
- **测试**：197 个测试，四个后端全绿，`moon check --deny-warn` 无警告。除了手算对照，`integration/` 里有一套**差分测试**：把每个稀疏例程和一份独立写的稠密实现对照（扁平行主序数组、部分主元高斯消去、另一套 Cholesky），两者不共用任何代码路径。这条最要紧——稀疏下标写错是静默的，`row_ptr` 错一项照样算出一个看着合理的数，只有第二个实现会当场翻脸。

## 六、原创或移植

原创，不是移植。没有对照任何现有项目的代码，也没有复制任何第三方源码。

## 七、参考来源信息

实现依据的是公开教科书里的标准算法，只参考算法描述，没有参考代码：

- Timothy A. Davis, *Direct Methods for Sparse Linear Systems*, SIAM, 2006 —— 消去树、reach、up-looking 稀疏 Cholesky、计数排序式的 CSR/CSC 互转。
- Yousef Saad, *Iterative Methods for Sparse Linear Systems*, 2nd ed., SIAM, 2003 —— 预条件共轭梯度、BiCGSTAB、重启 GMRES、ILU(0)。
- Matrix Market 格式规范（NIST），`%%MatrixMarket` 头、字段与对称性语义。

依赖只有 `moonbitlang/core`，没有引入任何第三方包。

## 八、GitHub 链接

https://github.com/lhw153766-ctrl/sparse-linalg

Apache-2.0。仓库根目录有 `COMPARISON.md`，逐条列出生态里已有的相关实现、依据（含 2552 个模块的注册表全量检索结果）以及本项目多出来的能力。
